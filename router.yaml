routes:

collections:
  /:
    permalink: /{slug}/
    template: index
    filter: tag:-de
  /de/:
    permalink: /de/{slug}/
    template: dehome
    filter: tag:de  

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
