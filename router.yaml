routes:


collections:
  /:
    permalink: /{slug}/
    template: index
    filter: tag:-hash-de
  /de/:
    permalink: /de/{slug}/
    template: dehome
    filter: tag:hash-de

taxonomies:
  author: /author/{slug}/
